/*
 * Compatibility wrapper.
 * Header.jsx là header duy nhất của hệ thống; file này không còn giữ một
 * bộ dữ liệu danh mục thứ ba để tránh lệch dữ liệu giữa các giao diện.
 */
import Header from "./Header";

export default Header;
